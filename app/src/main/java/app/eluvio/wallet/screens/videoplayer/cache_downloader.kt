package app.eluvio.wallet.screens.videoplayer

/*
        val cache = SimpleCache(File("downloads_i_guess"), NoOpCacheEvictor(), makeDbProvider())
        val cacheDataSourceFactory = CacheDataSource.Factory()
            .setCache(cache)
            .setUpstreamDataSourceFactory(DefaultHttpDataSource.Factory());
        // Create a downloader for the first representation of the first adaptation set of the first
        // period.
        val dashDownloader = DashDownloader(mediaItem, cacheDataSourceFactory);
        // Perform the download.
        dashDownloader.download { contentLength, bytesDownloaded, percentDownloaded ->
        }
        // Use the downloaded data for playback.
        return DashMediaSource.Factory(cacheDataSourceFactory).createMediaSource(mediaItem);

    */



//private fun makeDbProvider(): DatabaseProvider {
//    TODO("Not yet implemented")
//}
