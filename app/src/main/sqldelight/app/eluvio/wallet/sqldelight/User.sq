CREATE TABLE User (
  address TEXT PRIMARY KEY,
  foo TEXT -- placeholder to make Queries return User instead of String
);

getCurrentUser:
SELECT * FROM User
LIMIT 1 ;

insert:
REPLACE INTO User (address, foo)
VALUES (?,"foo");

deleteAll:
DELETE FROM User;
